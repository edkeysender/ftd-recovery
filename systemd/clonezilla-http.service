[Unit]
Description=Clonezilla HTTP server (serves filesystem.squashfs + ocs scripts to PXE clients)
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 -m http.server 8080 --directory /srv/tftp
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
